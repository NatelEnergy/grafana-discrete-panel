{
  "github": {
    "release": true,
    "assets": ["dist/*.zip"]
  },
  "scripts": {
    "afterBump": "yarn build; git add --verbose --force dist/; cd dist; zip -r natel-discrete-panel-${version}.zip *",
    "beforeStage": "echo STAGE ${name} v${version}",
    "afterRelease": "echo Successfully released ${name} v${version} to ${repo.repository}."
  },
  "npm": {
    "publish": false
  }
}
